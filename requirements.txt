fredapi==0.5.1
pandas==2.2.3
python-dotenv==1.0.1
requests==2.32.3
pytest==7.4.4
matplotlib==3.9.0
# Phase 2 additions
streamlit==1.35.0
plotly==5.22.0
# Phase 3 additions
# prophet 1.1.6 + cmdstanpy 1.2.4 is a known-good combination on Streamlit Cloud.
# prophet 1.1.5 triggers 'Prophet has no attribute stan_backend' when cmdstanpy
# is unpinned and resolves to >=1.3.0, which changed its internal API.
prophet==1.1.6
cmdstanpy==1.2.4
